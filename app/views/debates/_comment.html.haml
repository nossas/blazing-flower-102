.comment.clearfix
  = image_tag comment.member.image_url
  .right_column
    %p= link_to "#{comment.member.first_name} #{comment.member.last_name}", "#"
    %p.content= comment.content
    %p
      = distance_of_time_in_words comment.created_at, Time.now
      |
      = link_to "Flag", "#"
