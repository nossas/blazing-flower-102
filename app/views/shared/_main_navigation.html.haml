%nav#main
  %ul
    %li
      =link_to 'SOBRE O MEU RIO', page_path(:id => 'about'), :class => "top_level #{(@current_page ==  'about' ? 'selected' : '')}"
      |
    %li.issue_menu_wrap
      =link_to 'NA ATIVIDADE', '#', :class => "top_level #{(@current_page == 'issue' ? 'selected' : '')}"
      |
      -if nav_issues.count > 2
        .issues.wide.clearfix
          %ul.thumbnails
            - for issue in nav_issues.first(2) do
              %li.issue_pane
                =link_to image_tag(issue.thumbnail), issue
                =link_to issue.name, issue
          .list
            %span
              MORE ISSUES:
            %ul
              - for issue in nav_issues - nav_issues.first(2) do
                %li.issue_pane
                  =link_to issue.name, issue
      -else
        .issues.grid_3
          %ul.grid_3.alpha.thumbnails
            - for issue in nav_issues do
              %li.issue_pane
                =link_to image_tag(issue.thumbnail), issue
                =link_to issue.name, issue


    %li
      =link_to 'NOSSO BLOG', "#", :class => "top_level"
      |
    %li
      =link_to 'DANDO UMA FORÇA', "#", :class => "top_level last"
