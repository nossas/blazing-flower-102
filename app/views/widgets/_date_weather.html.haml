#date_weather.widget
  .date= raw I18n.l Time.zone.now, :format => :widget
  .icon
    =image_tag "#{weather_category}_#{day_or_night}.png"
  .max.forecast
    .label Max
    .temp #{WeatherWidget.weather['item']['forecast'][0]['high']}°
  .min.forecast
    .label Min
    .temp #{WeatherWidget.weather['item']['forecast'][0]['low']}°

