#new_user
  =link_to "Create a new user", new_admin_user_path
#users_list
  - @users.each do |user|
    -unless user == current_user
      = user.first_name
      = user.last_name
      = link_to "Edit", edit_admin_user_path(user), :id => "edit_user_#{user.id}"
