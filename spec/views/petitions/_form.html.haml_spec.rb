require "spec_helper"

describe "petitions/_form" do
  subject { rendered }

  context "user is logged in" do

    context "user has already signed the petition" do
    end

  end

  context "user is not logged in" do
  end

end
